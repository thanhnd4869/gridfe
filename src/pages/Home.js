/* eslint-disable no-param-reassign */
/* eslint-disable no-plusplus */
import Table from '../components/Table';

const Home = () => {
    return (
        <>
            <div>
                <Table />
            </div>
        </>
    );
};

export default Home;
